version: '3.1'
services:
  mongo:
    image: mongo
    restart: always
    container_name: mongo
    ports:
      - 8081:8081
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: mongo
